GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Upstash Redis (REST). Required for handover/PR/pair persistence.
# Auto-injected by the Vercel "Upstash for Redis" marketplace integration
# under the KV_REST_API_* names. For local dev, copy these from the Upstash
# database page in the Vercel dashboard (or from upstash.com → REST API tab).
KV_REST_API_URL=
KV_REST_API_TOKEN=
